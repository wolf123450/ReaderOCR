# Core runtime dependencies
Pillow>=10.0
numpy>=1.24
ebooklib>=0.18

# OCR engine (PaddleOCR)
# NOTE: paddlepaddle-gpu instead of paddlepaddle if a CUDA GPU is available.
paddlepaddle>=3.3
paddleocr>=3.4
